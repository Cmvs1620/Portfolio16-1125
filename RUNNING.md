# Running the React Portfolio

The actual app lives under `client/`.  
All commands below can be run from the **root** thanks to script forwarding.

```bash
npm install
npm run dev
npm run build
npm run preview
npm run lint
```

Alternatively, you can `cd client` and run the same commands there.
